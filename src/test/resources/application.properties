# Shared by @SpringBootTest so list/search/member mapping is verified with OSIV off
# (same as application-prod.yml) without forking an extra application context.
spring.jpa.open-in-view=false
spring.datasource.hikari.maximum-pool-size=4
